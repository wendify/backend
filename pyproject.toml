[project]
name = "backend"
version = "0.1.0"
dependencies = ["coloredlogs", "dotenv", "pandas", "plotly", "requests", "ruff"]

[project.scripts]
main = "main:main"

[tool.ruff]
line-length = 100

[tool.ruff.format]
indent-style = "tab"

[tool.ruff.lint]
extend-select = ["ANN", "I", "RSE"]
